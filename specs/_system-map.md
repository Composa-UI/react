<!-- GENERATED by tools/build-system-map.mjs. A v1 inventory to refine, not gospel.
     Dependency edges are scanned from h(Name) calls in factory.js; tiers are computed
     by dependency depth. Hand-refine tiers/edges as specs are written. -->

# Composa — system map (Phase 0 inventory)

The doc-first rebuild starts here: **what we have, what depends on what, what's missing.**
No spec is written until this map is trusted. `Spec` shows whether a `specs/<tier>/<name>.md`
exists yet (all missing at start). `Depends on` = components rendered inside it (from a
factory scan). Overlay primitives (`OverlayHost`/`Portal`/`Layer`) are plumbing and are noted
as _needs_, not counted toward tier depth.

**Counts:** 107 components — 34 atoms,
12 molecules,
61 organisms.
Specs written: 0 / 107.

## Foundations & tokens (write these FIRST — everything depends on them)

Not components — the substrate every spec references. Author as `specs/foundations/*.md`:
color · typography · spacing · radius · **elevation** · motion · z-index · iconography ·
accessibility · breakpoints · grid · borders · opacity. Token values live in `design/tokens.css`
(closed set; specs reference names, never raw values).

## Components by tier (write in this order: atoms → molecules → organisms)

### Atoms (depend on nothing but primitives)

| Component | Spec | Depends on | Notes |
|---|---|---|---|
| `Avatar` | — | — | |
| `Badge` | — | — | |
| `BlendModeMenu` | — | — | |
| `Button` | — | — | · 184 variants · UI3 Button, node 2012:46721 |
| `CanvasSelectionOverlay` | — | — | · 16 variants · UI2 Canvas decorations, node 0:20495 |
| `Checkbox` | — | — | · 12 variants · UI3 Checkbox, node 2012:55461 |
| `ChipVariable` | — | — | |
| `Chit` | — | — | |
| `ChitInput` | — | — | |
| `CollapseHeader` | — | — | |
| `ColorInput` | — | — | |
| `ComboInputDropdown` | — | — | |
| `ComposaAppIcon` | — | — | |
| `DialogBody` | — | — | |
| `DialogFooter` | — | — | |
| `DialogHeader` | — | — | |
| `EditorShell` | — | — | |
| `EffectValueButton` | — | — | |
| `ExportPreviewDisclosure` | — | — | |
| `HeaderActions` | — | — | |
| `Label` | — | — | |
| `MenuDivider` | — | — | |
| `NumericInputMulti` | — | — | |
| `Radio` | — | — | · 20 variants · UI3 Radio, node 2015:20365 |
| `SlideThumb` | — | — | |
| `Switch` | — | — | · 24 variants · UI3 Switch, node 2015:24697 |
| `Tab` | — | — | |
| `TextPair` | — | — | |
| `Tooltip` | — | — | · 16 variants · UI3 Tooltip, node 2015:32842 |
| `TreeRow` | — | — | |
| `TypographyFieldset` | — | — | |
| `TypographySpecimenSegmented` | — | — | |
| `VerticalCell` | — | — | |
| `VisualBell` | — | — | |


### Molecules (compose atoms)

| Component | Spec | Depends on | Notes |
|---|---|---|---|
| `BadgeAnchor` | — | ← Badge | |
| `ComboInput` | — | ← ComboInputDropdown | |
| `CommentItem` | — | ← Avatar | |
| `FloatingTooltip` | — | ← Tooltip | |
| `ListCell` | — | ← HeaderActions | |
| `MenuFooter` | — | ← Button | |
| `MenuHeadingCell` | — | ← Label | |
| `Notification` | — | ← Avatar | |
| `SlideList` | — | ← SlideThumb | |
| `SlidesEditorTemplate` | — | ← EditorShell | |
| `Tabs` | — | ← Tab | · 11 variants · UI3 Tabs, node 2015:25519, _Tab node 2015:27758 |
| `Tree` | — | ← TreeRow | |


### Organisms (compose molecules — product surfaces)

| Component | Spec | Depends on | Notes |
|---|---|---|---|
| `AlignmentPicker` | — | ← FloatingTooltip | |
| `DialogHeaderCell` | — | ← ListCell | |
| `DialogRow` | — | ← ListCell | |
| `IconButton` | — | ← FloatingTooltip | · 62 variants · UI3 Button icon, Button icon toggle, Button icon dialog toggle, node 2012:46721 |
| `MenuRow` | — | ← MenuDivider, MenuFooter, Label | · 63 variants · UI3 Menu, node 2028:86486 |
| `CommentComposer` | — | ← IconButton, ChitInput, Avatar | |
| `ControlGroup` | — | ← IconButton | |
| `Dialog` | — | ← Button, IconButton, DialogHeaderCell, DialogHeader, DialogBody, DialogFooter | · 6 variants · UI3 Modal (Modal header / Modal body / Modal footer), Templates/320 (Basic, Advanced), Templates/480 (Embed, Create Project, Create Team, Sharing) |
| `Dropdown` | — | ← MenuRow | · 16 variants · UI3 Dropdown, node 2028:36589 |
| `Header` | — | ← IconButton, ListCell | |
| `InputField` | — | ← FloatingTooltip, IconButton | · 119 variants · UI3 Inputs, node 2028:75376 |
| `Menu` | — | ← MenuRow, SubmenuRow | |
| `MenuMultiSelect` | — | ← MenuRow | |
| `MultiplayerControl` | — | ← FloatingTooltip, IconButton, Avatar | |
| `NavigationRailItem` | — | ← IconButton | |
| `NavigatorHeader` | — | ← IconButton | |
| `NotificationBell` | — | ← IconButton | |
| `SectionValueButton` | — | ← IconButton, HeaderActions | |
| `SegmentedControl` | — | ← IconButton | · 20 variants · UI3 Segmented control, node 2015:20960 |
| `SplitButton` | — | ← FloatingTooltip, IconButton | |
| `StrokeAlignMenu` | — | ← MenuRow | |
| `ToggleButton` | — | ← IconButton | |
| `TypeStyleRow` | — | ← IconButton | |
| `ZoomMenu` | — | ← MenuDivider, MenuRow | |
| `AppearanceSection` | — | ← IconButton, InputField, Dropdown, HeaderActions, BlendModeMenu | |
| `AppNavigationRail` | — | ← ComposaAppIcon, NavigationRailItem | |
| `AutoLayoutSettingsDialog` | — | ← IconButton, Dropdown, SegmentedControl | |
| `ColorPickerDialog` | — | ← Button, IconButton, InputField, Dropdown, HeaderActions, Tabs | |
| `CommentThreadWindow` | — | ← CommentComposer, CommentItem | |
| `EditorNavigator` | — | ← IconButton, NavigatorHeader, CollapseHeader, Tree | |
| `EditorToolbarGroup` | — | ← IconButton, SplitButton, MenuRow | |
| `ExportSettingsDialog` | — | ← IconButton, InputField, Checkbox, Dropdown | |
| `FontsPickerMenu` | — | ← IconButton, InputField, Dropdown, HeaderActions | |
| `InspectorHeader` | — | ← Button, SplitButton, Dropdown, ListCell, Tabs, MultiplayerControl, ZoomMenu | |
| `LayerHeader` | — | ← IconButton, Dropdown | |
| `NumericInput` | — | ← InputField | |
| `PaintSection` | — | ← IconButton, ColorInput, Header | |
| `SelectionColorSummary` | — | ← IconButton, HeaderActions, SectionValueButton | |
| `SlidesNavigator` | — | ← IconButton, NavigatorHeader, SlideList | |
| `StrokeSettingsDialog` | — | ← IconButton, InputField, Dropdown | |
| `SubmenuRow` | — | ← MenuRow, Menu | |
| `TypeStyleMenu` | — | ← IconButton, InputField, HeaderActions, TypeStyleRow | |
| `TypographyAxisSlider` | — | ← InputField | |
| `TypographyControlRow` | — | ← SegmentedControl, TypographySpecimenSegmented | |
| `TypographyNumberRow` | — | ← InputField | |
| `ColorPickerTrigger` | — | ← ColorPickerDialog | |
| `EditorToolbar` | — | ← EditorToolbarGroup | |
| `EffectsSection` | — | ← IconButton, HeaderActions, PaintSection, EffectValueButton | |
| `ExportRow` | — | ← IconButton, ComboInput, Dropdown, HeaderActions, ExportSettingsDialog | |
| `ExportSection` | — | ← Button, PaintSection, ExportPreviewDisclosure | |
| `FillSection` | — | ← PaintSection | |
| `LayoutGuideSection` | — | ← PaintSection | |
| `LayoutSection` | — | ← IconButton, ToggleButton, NumericInput, Checkbox, HeaderActions, SegmentedControl, AlignmentPicker, AutoLayoutSettingsDialog | |
| `PositionSection` | — | ← IconButton, NumericInput, ControlGroup | |
| `SelectionColorsSection` | — | ← PaintSection, SelectionColorSummary | |
| `StrokeControlsSection` | — | ← IconButton, InputField, Dropdown, HeaderActions, StrokeSettingsDialog, StrokeAlignMenu | |
| `TypographyToggleRow` | — | ← TypographyControlRow | |
| `LayoutGuideSettingsDialog` | — | ← IconButton, InputField, Dropdown, ColorPickerTrigger | |
| `StrokeSection` | — | ← PaintSection, StrokeControlsSection | |
| `TypographyDialog` | — | ← IconButton, InputField, Dropdown, Tabs, FontsPickerMenu, TypographyControlRow, TypographyToggleRow, TypographyNumberRow, TypographyFieldset, TypographyAxisSlider | |
| `EditingInspector` | — | ← InspectorHeader, LayerHeader, PositionSection, EffectsSection, FillSection, StrokeSection, SelectionColorsSection, LayoutGuideSection, ExportSection, AppearanceSection, LayoutSection | |


## Infrastructure primitives

`OverlayHost` · `OverlayPortal` · `OverlayLayer` — the portal/mount plumbing every
tooltip, menu, and dialog depends on. Spec these alongside foundations; they're a hard
dependency for a large share of the tree.
